<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="work.work4.mapper.MessageMapper">
    <select id="selectByChatId" resultType="Messages">
        select id,chat_id,from_user_id,content,seq,is_read,type,publish_time
        from message
        where chat_id=#{chatId}
        and
        from_user_id!=#{userId}
    </select>
</mapper>